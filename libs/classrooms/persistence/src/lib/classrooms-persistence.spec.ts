import { classroomsPersistence } from './classrooms-persistence';

describe('classroomsPersistence', () => {
  it('should work', () => {
    expect(classroomsPersistence()).toEqual('classrooms-persistence');
  });
});
