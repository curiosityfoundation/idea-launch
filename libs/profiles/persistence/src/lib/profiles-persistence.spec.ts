import { profilesPersistence } from './profiles-persistence';

describe('profilesPersistence', () => {
  it('should work', () => {
    expect(profilesPersistence()).toEqual('profiles-persistence');
  });
});
