import { resourcesPersistence } from './resources-persistence';

describe('resourcesPersistence', () => {
  it('should work', () => {
    expect(resourcesPersistence()).toEqual('resources-persistence');
  });
});
